# 기기 없는 통합 점검 — 2026-09-15

## 원본 제품 소스 목록

`python scripts/audit_upstream_inventory.py`로 로컬 Git에 있는 고정 커밋과 현재 작업 파일을 대조했다.
원본 객체가 없으면 오류로 중단한다. 네트워크에서 임의로 최신 코드를 가져오거나 덮어쓰지 않는다.
파일별 원본 blob과 대응 후보/해시는 `build/upstream-source-inventory.json`에 기록한다.

| 원본 | 커밋 | src/main Kotlin/Java | 개행 제외 동일 | 변경 검토 대상 | 같은 클래스명 후보 없음 |
|---|---|---:|---:|---:|---:|
| Agent | c514105 | 113 | 78 | 34 | 1 |
| 검색·멀티턴 | 734c63b | 60 | 15 | 23 | 22 |
| OCR App | 010d3b1 | 5 | 0 | 5 | 0 |

합계 178개. 마지막 열은 **기능 누락 건수나 미구현 건수가 아니다**.
같은 경로를 우선 찾고 그 외에는 클래스 이름을 비교하므로 Java→Kotlin 전환도 후보로 잡는다.
이름이 같은 파일도 기능 동등성이 입증된 것은 아니다. 이 목록은 네이티브 코드·자산·매니페스트·빌드 설정·
Python 실험/참조 실행기까지 전수 검증한 보고서가 아니다. 의미적 동등성 완료 표시도 하지 않는다.

이번에 직접 확인한 대응/차이:

- 원본 `AgentViewModel`의 UI 역할은 `ChatSession`으로 이동했다.
- 검색 원본의 `ConversationMemoryReducer.kt`는 동일 파일명이 없지만
  통합 `agent-core/.../AgentSession.kt` 안에 `ConversationMemoryReducer`가 있다.
- Room 데이터 파일 5개는 Java→Kotlin으로 바뀌어 기존 확장자까지 포함한 단순 목록 비교에서 누락처럼 보였다.
- 원본 `RoomFtsKeywordRetriever`는 TODO와 빈 결과 반환만 있는 미연결 구현이다.
  실제 앱은 `RoomBusinessCardRepository.searchKeywordCandidates` → `RyeongContactSearchBackend`를 사용한다.
- 원본 `HuggingFaceTokenizer.tokenizeForModel`은 런타임 미연결 예외를 던지는 구현이다.
  통합앱의 실제 토크나이저/임베딩 경로는 `AndroidEmbeddingGemmaEngine`이다.
- 원본 `NoOpRerankerEngine`은 모델이 없으며 목록을 자르는 구현이다.
  원본 SearchLookupService의 해당 호환 생성자도 `RerankerEngine unused`로 받는다.
  이 파일을 복사한다고 누락된 신경망 재순위화 모델이 복원되는 것이 아니다.
- OCR `CardParser`, `Postprocess`의 줄별 차이는 패키지와 설명 주석이었다.
  KIE/OcrPipeline/화면 코드는 실제 통합 변경이 있으므로 같은 파일명의 존재만으로 동등 판정을 하지 않았다.

나머지 변경 파일과 대응 후보의 세부 동작 대조는 열린 점검 항목이다.

후속 검토에서 위 23개 이름 불일치 파일을 모두 `upstream_replacements.json`에 대체/이동/의도적 제외로
분류하고 대응 파일 존재를 검사했다. 매 실행에서 미분류 파일이나 사라진 대체 파일이 있으면 오류로 중단한다.
이는 구조적 대응의 완료이며 모든 입력에 대한 동작 동등성의 증명은 아니다.

## 기능 단위 후속 검토

| 기능군 | 정적 대조 및 연결 확인 | 회귀/추가 증거 |
|---|---|---|
| 검색 정규화·이름/지역/직함 | QueryAnalyzer, SearchFieldVocabulary/Resolver 및 원본 후보/가제티어 대응 확인 | 기존 UpstreamParityCharacterization, SearchFieldConstraintResolver/Characterization 시험 |
| 회사 조건·복합 조건 | 원본 회사 hard filter가 통합 plan에서 누락된 것을 확인, 알려진 전체 회사명 축 추가 | CompanyConstraintIntegration: 메모 오매칭, 긴 회사명 접두어, 회사+부서, 조사, 인원수, 지명 충돌 |
| 부서 조건 | resolver와 matches에는 있었으나 apply가 지역 없는 부서 필터를 우회 | 수정 전 3개 실패 재현; DepartmentConstraintIntegration 재검증 |
| 키워드·벡터·RRF | Room FTS 4단계→후보 adapter→SearchLookupService→RRF. 모델 누락 폴백 차이는 사용자 요구에 따른 의도적 변경 | 기존 검색 production-adapter 시험, 실제 호스트 임베딩 추론, APK 색인 검사 |
| 임베딩 갱신·저장 | 저장/수정 callback→색인 무효화·새 벡터 저장 후 완료. 데이터와 벡터 일관성 검사 경로 확인 | 기존 ContactPlugins 및 Room 계측 시험 코드; 후자는 기기 실행과 구분 |
| 멀티턴 상태·대상·교정 | 원본 독립 memory/reducer→AgentSession 내 reducer, ordinal/correction/history 라우팅으로 대응 | 기존 AgentSessionManager, TurnRace, RouterGeneralization, RecoveredUpstreamBehaviour 시험 |
| 에이전트 실행·안전 정책 | typed registry, fresh get, 확인/권한, native tool reply 순서 및 세션 세대 경로 확인 | 기존 AgentKernel/StructuredAgentKernel/WorkflowPolicy/NativeToolReplyTracker 시험 |
| UI 세션·채팅 증거 | AgentViewModel→ChatSession; 탭 검색은 기록기 분리, reset/cancel 세대 분리 | ChatSessionConcurrency 및 이번 직접 조회 기록 검사 |
| OCR 검출/인식/KIE/후처리 | Android bitmap/EXIF 계층과 core-ocr 분리, KIE/방향 모델 필수 처리, 그룹화·매핑 경로 | 기존 OCR 그룹화/mapper 시험, 호스트 실제 모델 smoke |
| 빌드/자산/플랫폼 연결 | 공통 모듈 의존성, Manifest의 앱 수명·singleTop·FileProvider·외부 intent 가시성 확인 | 전체 APK 빌드 및 APK 내 11개 자산 SHA-256/arm64 네이티브 6개/1000벡터 검사 |

관측 누락도 수정했다. SearchPlanSnapshot이 실제 적용한 부서를 항상 빈 배열로 보고했으며,
회사 축을 추가한 뒤 회사·부서·지역·직함을 실제 backend 응답에 실어 계측 평가까지 전달한다.
외부 관측 콜백이 실패해도 검색 결과와 내부 관측값은 보존된다.

## 실제 PC 모델 실행 결과

`scripts/check_host_models.py`는 필수 모델/런타임 파일과 배포 모델 3개 해시를 먼저 검사하고,
없거나 로드/추론 실패하면 즉시 실패한다. 다운로드/키워드 폴백/skip은 하지 않는다.

- Windows AMD64, 설치된 LiteRT-LM Python 0.13.1 + DLL에서 실제 Gemma-4-E2B 실행.
  `2 더하기 3` 질문의 실제 응답은 `5`.
- 실제 EmbeddingGemma ONNX export 실행: 768차원, 유한값, 벡터 norm 1.0.
- APK와 같은 OCR det/rec/textline_ori ONNX 파일로 실제 추론 실행: 출력 shape/유한값 검사 통과.
  입력은 빈 이미지 텐서이므로 문자인식 정확도 통과를 뜻하지 않는다.
- APK와 같은 KIE tokenizer/classifier/labels로 계측 시험의 10개 텍스트 샘플 추론: 10/10 일치.
- 원시 증거: `build/host-model-smoke/run-1789452251417877500.jsonl`.

**호스트 실행은 Android 통합앱의 모델 실행과 별도다.** Android는 LiteRT-LM 0.16.1/RAG TFLite 런타임을 쓴다.
PC 임베딩은 ONNX export이므로 Android TFLite와 벡터가 완전히 동일하다고 주장하지 않는다.
전체 435턴, 실제 이미지 OCR 정확도, 외부 앱 동작까지 검증한 결과도 아니다.

## 확인한 평가 결함 및 수정

1. 165개 실행기가 gold를 보관만 해 잘못된 명함 검색도 답변 문구가 맞으면 통과할 수 있었다.
   실제 검색 결과의 Hit@5/제한 Recall@5/reciprocal rank와 직접 조회의 gold 교집합 검사를 추가했다.
2. 직접 조회 결과를 관측할 수 없어 검색 결과와 구분하기 어려웠다.
   RecordingContactSearchBackend가 성공한 get ID와 검색 실행 여부를 현재 턴에 한정해 기록한다.
   이전 턴에서 늦게 완료된 get은 다음 턴의 증거에 포함하지 않는다. UI에 붙는 검색 카드 목록은 바꾸지 않는다.
3. 매 턴 `actions.lastOrNull()`을 읽어 오류 턴에 이전 action을 기록할 수 있었다.
   이번 `TurnStarted.turnId`와 일치하는 action만 기록하도록 바꿨다.
4. 기억 기반 답변 등의 미채점 항목을 검색 성공으로 간주하지 않는다.
   coverage gap/미채점 턴 수/전체 품질 축 미완료 상태를 결과에 남긴다.

이번 검사는 채점 코드와 기록기의 제어 흐름을 검증한다. 모델을 대신하는 테스트 결과가 아니다.
원본 질문·gold·must/must_not는 수정하지 않았다.

## 최종 회귀 검증

- `test :app:assembleDebug :app:assembleDebugAndroidTest`: BUILD SUCCESSFUL.
- 단위 테스트 XML 집계: 559개, 실패 0, 오류 0, 건너뜀 0.
- `export_multiturn_app_suite.py --check`: 165개 시나리오/435턴 원본 fixture 일치.
- `audit_upstream_inventory.py`: 고정 커밋의 제품 소스 178개 대조, 이름 불일치 23개 모두 분류 및 대체 파일 존재 확인.
- `check_apk_payload.py`: 현재 소스 자산 11개 해시 일치, arm64 라이브러리 6개, 1000개 × 768차원 색인 검사 통과.
- 빌드 로그: `build/equivalence-final.log`. APK 검사 증거: `build/apk-payload-check.json`.

모델 smoke 결과는 위의 실제 PC 실행 범위에 한정한다. 단위 테스트 559개에 실기기 계측 시험을 포함하지 않았다.

## 아직 남은 범위

- 변경 구현의 경계 입력별 추가 동등성 검증(정적 대응과 기존 회귀의 통과가 전 입력 증명은 아님).
- 기억 기반 답변과 count/clarification의 gold 검증, 이름/focus/옛 경로 의미 차이의 채점 규칙.
- 실제 UI no_cards/승인·외부 작성 화면·새 대화 동작 검증.
- 필수 모델을 모두 올린 실제 OCR·검색·435턴 실행 및 실패 수정.

빌드/단위 테스트 성공과 실모델 품질·원본 완전 통합 판정은 별개다.

## 후속 S21 준비 점검 — 앱 다운로드 검증으로 전환

- Galaxy S21(SM-G991N)에 무선 ADB 연결 후, 사용자 요청에 따라 기존 앱과 앱 전용 모델 4개(구 FunctionGemma 포함)를 제거했다.
- 깨끗한 설치에서 Windows PowerShell 5.1의 빈 native 출력 처리와 adb push의 성공 stderr 처리 오류를 발견했다.
  설치 스크립트는 빈 출력을 문자열로 합치고, 전송/계측 명령은 종료 코드로 판정하도록 수정했다.
  `scripts/test_real_device_installer.ps1`의 구문·정상 stderr·실패 종료 코드·빈 출력 검사를 통과했다.
- 사용자가 앱 다운로드 경로 검증을 선택해 PC 모델 복사를 중단했고, 앱과 복사 중이던 파일의 삭제를 확인했다.
  이후 APK만 설치하고 앱의 모델 다운로드 화면에서 진행한다. PC 복사를 앱 다운로드 검증으로 간주하지 않는다.
- 아직 실기기 모델 추론/435턴 결과는 없다. Hugging Face 이용 동의·읽기 토큰은 사용자가 휴대폰에서 직접 입력한다.
  토큰을 수집하거나 기록하지 않는다. 실제 테스트 종료 후 앱·테스트 패키지·다운로드 모델도 제거하고 결과만 PC에 보관한다.
